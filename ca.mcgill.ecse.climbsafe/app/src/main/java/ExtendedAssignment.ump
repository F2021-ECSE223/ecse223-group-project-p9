use ClimbSafe.ump;

class Assignment {
	enum TripStatus {Started, Finished, Cancelled, Assigned};
	lazy TripStatus tripStatus;
	lazy int totalPayment;
	lazy String paymentCode;
	lazy boolean banned;
	lazy boolean fullyPaid;
}