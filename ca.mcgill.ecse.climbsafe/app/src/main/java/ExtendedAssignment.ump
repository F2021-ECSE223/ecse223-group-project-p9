use ClimbSafe.ump;

class Assignment {
	enum TripStatus {Started, Ended, Cancelled, Standby};
	lazy TripStatus tripStatus;
	lazy int totalPayment;
	lazy string paymentCode;
	lazy boolean banned;
	lazy boolean fullyPaid;
}